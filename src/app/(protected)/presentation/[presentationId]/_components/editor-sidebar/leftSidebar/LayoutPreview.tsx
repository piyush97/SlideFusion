import { useSlideStore } from "@/store/useSlideStore";

type Props = {};

const LayoutPreview = (props: Props) => {
  const {} = useSlideStore();
  return <div>LayoutPreview</div>;
};

export default LayoutPreview;
